function longestStr(args) {
    let words = args.split(' ');
    let max = 0;
    let tmp = 0;
    for (let i = 0; i < words.length; i++){
        if (words[i].length > max) {
            max = words[x].length;
            tmp = x;
        }
    }

    return arrayOfWords[tmp];
}
